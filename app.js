//name
const name = "Joshua Adesanya";

// Courses
const courses = ["Html", "CSS", "Javascript", "php", "nodejs", "golang", "C#"];

//display name
console.log(name);

//display course
console.log(courses);

//display all odd numbers from 0 to 200 inclusive
for (let odd = 1; odd <= 200; odd++) {
  if (odd % 2 !== 0) {
    console.log(odd);
  }
}
